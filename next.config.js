/** @type {import('next').NextConfig} */
const nextConfig = {
    experimenal:{
        appDir: true
    },
    images: {
        domains:["upload.wikimedia.org"]
    }
}

module.exports = nextConfig
